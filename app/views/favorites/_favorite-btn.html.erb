<% if book.favorited_by?(current_user) %>
  <%= link_to "♥#{book.week_favorites.count}いいね", book_favorite_path(book.id), method: :delete, remote: true %>
<% else %>
  <%= link_to "♡#{book.week_favorites.count}いいね", book_favorite_path(book.id), method: :post, remote: true %>
<% end %>
